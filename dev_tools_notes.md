# Developer Tooling Notes

## Environment Variables

Environment variables configure behavior without hardcoding values.

Example:

APP_ENV=development

## Curl

curl lets you send HTTP requests from the terminal.

Example:

curl https://api.github.com

## Processes

A process is a running program.

Useful commands:

ps aux
grep
kill
top
